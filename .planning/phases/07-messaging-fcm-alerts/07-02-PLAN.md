---
wave: 2
depends_on: ["07-01"]
files_modified: [
  "mobile_app/lib/services/fcm_service.dart",
  "mobile_app/main.dart",
  "mobile_app/lib/features/shared/providers/notifications_provider.dart"
]
autonomous: true
requirements_addressed: [REAL-TIME]
---

# Plan 07-02: FCM Service & Background Handling (Mobile)

Implement the push notification listener in the Flutter app.

<objective>
To setup `firebase_messaging` in the Parent and Teacher apps to receive alerts in foreground, background, and terminated states.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/pubspec.yaml"] </read_first>
<action> Add `firebase_messaging` and `flutter_local_notifications` to `pubspec.yaml`. </action>
<acceptance_criteria> Plugins are correctly registered and linked. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Create `mobile_app/lib/services/fcm_service.dart`. Implement `initializeNotifications` with foreground/background handlers and token refresh logic. </action>
<acceptance_criteria> App correctly requests notification permissions on startup. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/main.dart"] </read_first>
<action> Setup FCM on background handler in `main.dart` for terminated-app alerts. </action>
<acceptance_criteria> Background messages are correctly received and shown as OS-level notifications. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/shared/providers/notifications_provider.dart` using Riverpod to track unread count in real-time. </action>
<acceptance_criteria> Dashboards update their "Notification Bell" count instantly. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that the device token is successfully generated and logged to the console.
2. verify that background messages trigger an OS banner notification.
3. confirm that if the app is open (foreground), a custom top-bar toast appears instead of the OS banner.
</verification>

<must_haves>
- Termination-to-app-interaction path correctly handled.
- Deep-linking from notification to specific View (e.g. Absences view).
- Foreground toasts for live interaction.
</must_haves>
