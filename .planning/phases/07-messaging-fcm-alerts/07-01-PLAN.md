---
wave: 1
depends_on: []
files_modified: [
  "backend/src/services/notificationService.js",
  "backend/src/models/Notification.js",
  "backend/src/controllers/attendanceController.js"
]
autonomous: true
requirements_addressed: [REAL-TIME]
---

# Plan 07-01: Automated Dispatch Engine (Backend)

Implement the push notification dispatch layer to alert parents of school events.

<objective>
To create a background service that triggers FCM push notifications for specific events: (a) Student absence, (b) New Homework, and (c) Result updates.
</objective>

<tasks>
<task>
<read_first> ["backend/src/app.js"] </read_first>
<action> Initialize `firebase-admin` in `backend/src/services/notificationService.js`. Add a `sendPush` function that accepts `schoolId`, `token`, and `payload`. </action>
<acceptance_criteria> `firebase-admin` is correctly configured with credentials. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Create `backend/src/models/Notification.js` to log all sent messages in the database with `school_id` and `student_id` scoping. </action>
<acceptance_criteria> In-app history depends on this persistent record. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/controllers/attendanceController.js"] </read_first>
<action> Hook into `attendanceController.postAttendance`. If a student is marked 'Absent', call `notificationService.sendPush` to the parent's registered device token. </action>
<acceptance_criteria> Every absence mark triggers an automated alert dispatch. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/controllers/homeworkController.js"] </read_first>
<action> Hook into `homeworkController.postHomework`. Dispatch a notification to the entire class/section targeting their parents' tokens. </action>
<acceptance_criteria> Parents are alerted as soon as new homework is posted. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that triggering a mock absence for a student calls the FCM dispatch function.
2. verify that notification logs are correctly populated in the `notifications` table.
3. verify that multi-tenant isolation is maintained: only parents of target students get their specific alerts.
</verification>

<must_haves>
- Automated Triggers: Seamlessly integrated into existing controllers.
- Logging: Every notification sent must be archived for in-app viewing.
- Role isolation: Parents only receive notifications for their own children.
</must_haves>
