---
wave: 2
depends_on: ["04-01"]
files_modified: [
  "mobile_app/lib/features/teacher/views/teacher_timetable_view.dart",
  "mobile_app/lib/features/teacher/widgets/timetable_grid_cell.dart",
  "mobile_app/lib/features/teacher/providers/teacher_timetable_provider.dart"
]
autonomous: true
requirements_addressed: [T-ACT-02]
---

# Plan 04-02: Weekly Timetable UI

Implement the core weekly planning interface for teachers in Flutter.

<objective>
To create a high-fidelity, interactive weekly timetable grid that allows teachers to plan ahead and see their full workload.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/teacher/providers/teacher_schedule_provider.dart"] </read_first>
<action> Setup `mobile_app/lib/features/teacher/providers/teacher_timetable_provider.dart` with a Riverpod `FutureProvider` fetching the full 5-day grid. </action>
<acceptance_criteria> `teacherTimetableProvider` returns a mapped map of days (Mon-Fri) to lists of periods for the teacher. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/views/teacher_timetable_view.dart` using a horizontal page-view or grid scroll for days. </action>
<acceptance_criteria> Weekly view follows the Teacher Purple-Indigo primary theme. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/teacher/widgets/timetable_grid_cell.dart` displaying the class, subject, and time for a specific period. </action>
<acceptance_criteria> Grid cells are color-coded: Current (Highlighted), Completed (Dimmed), Future (Standard). </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/views/teacher_dashboard_view.dart"] </read_first>
<action> Link the "Full Timetable" text-button on the dashboard to this new view. </action>
<acceptance_criteria> Navigation from Dashboard -> Timetable is smooth and preserves state. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Run app and confirm the full weekly grid loads correctly for a teacher.
2. verify that today's column is automatically centered or highlighted in the scroller.
3. Test empty period states (free periods) show a clean "Free Period" label.
</verification>

<must_haves>
- 5-Day Grid: Clear visualization of Mon-Fri.
- Interaction: Clicking a cell should allow Quick Actions (Attendance/HW).
- Readability: High-contrast typography for subject names in the grid.
</must_haves>
