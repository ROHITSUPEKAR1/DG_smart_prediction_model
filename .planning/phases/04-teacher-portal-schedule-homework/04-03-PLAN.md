---
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: [
  "mobile_app/lib/features/teacher/views/add_homework_view.dart",
  "mobile_app/lib/features/teacher/widgets/multi_class_selector.dart",
  "mobile_app/lib/features/teacher/widgets/attachment_scroller.dart",
  "mobile_app/lib/features/teacher/providers/homework_form_provider.dart"
]
autonomous: true
requirements_addressed: [T-CORE-03, T-CORE-04]
---

# Plan 04-03: Homework Assignment UI

Implement the core homework creation interface for real-time tasking.

<objective>
To design a high-fidelity, interactive homework assignment system that handles multi-input (caption + photo) and targeting across student groups.
</objective>

<tasks>
<task>
<read_first> [] </read_first>
<action> Setup `mobile_app/lib/features/teacher/providers/homework_form_provider.dart` with Riverpod `StateNotifier` that manages the homework payload state (caption, file list, target IDs). </action>
<acceptance_criteria> `homeworkFormProvider` correctly stores text input and allows toggling multiple class IDs. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/widgets/multi_class_selector.dart` with a horizontal scroller showing assigned divisions (10-A, 10-B, etc.). </action>
<acceptance_criteria> Tapping a class toggles its selection in the `homeworkFormProvider` payload. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/widgets/attachment_scroller.dart` with buttons for Camera / Photo Library. </action>
<acceptance_criteria> Scroller displays thumbnail previews of selected photos/files. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/views/teacher_dashboard_view.dart"] </read_first>
<action> Finalize `mobile_app/lib/features/teacher/views/add_homework_view.dart`. Combine Selector, Input, and Scroller. </action>
<acceptance_criteria> Navigation from the Dashboard/Timetable -> Add Homework works. Clicking "Finalize & Post" calls `POST /api/homework/add`. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that selecting multiple classes correctly updates the 'Post to' label.
2. verify that clicking the camera icon (mock) adds a placeholder image thumbnail in the scroller.
3. Test layout on small devices to ensure the scroller doesn't break.
</verification>

<must_haves>
- Hybrid Input: Combined text and photo upload for speed.
- Batch Targeting: Post to 2+ classes in one flow.
- Success Feedback: Confetti/SnackBar on successful post to confirm FCM was sent.
</must_haves>
