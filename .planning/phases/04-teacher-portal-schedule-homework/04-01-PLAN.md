---
wave: 1
depends_on: []
files_modified: [
  "backend/src/models/Homework.js",
  "backend/src/models/Resource.js",
  "backend/src/controllers/homeworkController.js",
  "backend/src/routes/homework.js"
]
autonomous: true
requirements_addressed: [T-CORE-03]
---

# Plan 04-01: Homework & Resource Backend

Implement the database entities and endpoints for assigning homework and sharing resources.

<objective>
To build a scalable backend API for teachers to post academic tasks and resources with multi-class targeting and file attachment support.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/BaseModel.js"] </read_first>
<action> Create models `Homework.js` and `Resource.js`. Implement `createHomework` with multi-class support (iterative insertion or student-visibility joins). </action>
<acceptance_criteria> `Homework.js` accepts an array of class IDs and persists the record for each targeted division. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `backend/src/controllers/homeworkController.js`. Add `addHomework` featuring caption + file meta storage. </action>
<acceptance_criteria> `POST /api/homework/add` successfully records the task and returns a success status. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Add `getTeacherHomework` to list all assignments posted by the logged-in teacher. </action>
<acceptance_criteria> Endpoint returns a history of homework tasks with "Engagement" counts (read/unread). </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/app.js"] </read_first>
<action> Mount the new homework routes in `app.js` with teacher-role protection. </action>
<acceptance_criteria> New routes are accessible behind JWT authentication. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Mock teacher login and call `POST /api/homework/add` with multiple target classes.
2. verify that the homework appears in the history of all targeted student groups.
3. Confirm that files are correctly metadata-recorded in the database (actual storage handled in Wave 2/Mobile).
</verification>

<must_haves>
- Multi-Class Support: Assigning a task to multiple divisions in one request.
- Metadata storage: Support for caption and file URLs (placeholder).
- Teacher Isolation: Teachers only see the homework they posted.
</must_haves>
