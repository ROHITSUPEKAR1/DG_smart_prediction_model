---
wave: 3
depends_on: ["09-01", "09-02"]
files_modified: [
  "mobile_app/lib/features/teacher/views/class_result_export_view.dart",
  "mobile_app/lib/features/teacher/widgets/export_button.dart"
]
autonomous: true
requirements_addressed: [T-CORE-04]
---

# Plan 09-03: Class Record & PDF Export

Provide offline archive capabilities for the school administration/teachers.

<objective>
To implement a reporting tool within the teacher hub that generates a consolidated PDF table of a class's result for archiving or printing purposes.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/pubspec.yaml"] </read_first>
<action> Add `pdf` and `printing` packages to `pubspec.yaml`. Provide standard dependencies. </action>
<acceptance_criteria> `pdf` layout tools are available for generation. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Create `mobile_app/lib/features/teacher/widgets/export_button.dart` in the Academic Hub that triggers a Riverpod call to fetch ALL subject results for a `class_id` and structured as a pivot table (Student rows, Subject columns). </action>
<acceptance_criteria> Matrix transformation works efficiently to build the report context. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/teacher/views/class_result_export_view.dart`. Builds a `pw.Document()` with columns automatically inferred from the dataset and rows for each student. Includes the DG Smart school header logo. </action>
<acceptance_criteria> The PDF opens via native `Printing.layoutPdf` sheet for sharing/saving. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that the generated PDF table perfectly aligns students vertically and subjects horizontally.
2. verify that hitting the export button successfully loads the OS-level save/print dialog.
3. verify that the 'Total Percentage' and 'Grade' are appended as the final two columns.
</verification>

<must_haves>
- Professional Formatting: Minimalistic PDF layout suited for physical printing (black/white contrast).
- Aggregation: Handles subjects seamlessly, inferring columns based on taught subjects.
- Quick Share: Integration with OS share sheet via the `printing` package.
</must_haves>
