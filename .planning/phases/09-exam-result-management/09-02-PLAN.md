---
wave: 2
depends_on: ["09-01"]
files_modified: [
  "mobile_app/lib/features/teacher/views/academic_control_hub.dart",
  "mobile_app/lib/features/teacher/widgets/audit_subject_card.dart",
  "mobile_app/lib/features/teacher/providers/academic_control_provider.dart"
]
autonomous: true
requirements_addressed: [T-CORE-04]
---

# Plan 09-02: Academic Control Hub UI (Mobile)

Build the "Draft vs Published" management interface for teachers.

<objective>
To create a high-level administrative view for Teachers/Admins where they can see completion statuses for subjects and officially "Publish" results to trigger Parent alerts.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/pubspec.yaml"] </read_first>
<action> Create `mobile_app/lib/features/teacher/providers/academic_control_provider.dart`. Setup Riverpod to fetch the audit status of all subjects for a term. </action>
<acceptance_criteria> `academicControlProvider` fetches Draft vs Published states. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/views/academic_control_hub.dart`. A vertical list grouping subjects by term, showing clear progress indicators. </action>
<acceptance_criteria> The view is styled minimally, focusing on data clarity. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/teacher/widgets/audit_subject_card.dart`. Displays the number of students graded vs pending, and a prominent "PUBLISH" button. </action>
<acceptance_criteria> "Publish" button triggers the `resultController.publishResults` endpoint with a confirmation dialog. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/widgets/teacher_quick_actions.dart"] </read_first>
<action> Update "Exams" or "Post Marks" to navigate to this new Control Hub, treating it as the gateway to the existing spreadsheet entry. </action>
<acceptance_criteria> Flow: Dashboard -> Control Hub -> (Select Subject) -> SpreadSheet Entry AND Publish. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that hitting 'Publish' throws a confirmation dialog preventing accidental releases.
2. verify that clicking a subject on the Hub correctly loads the existing `MarksEntryView` from Phase 6.
3. verify that the progress bar accurately reflects graded/total students.
</verification>

<must_haves>
- Irreversible UI Feedback: Ensure the "PUBLISH" action is heavily guarded with an "Are you sure?" modal, as it sends FCMs to 40+ parents instantly.
- Hierarchy Integration: Serves as the new parent-view for the Phase 6 marks entry.
- Minimal Styling: Shift from vibrant dashboard colors to professional administrative grays/blues.
</must_haves>
