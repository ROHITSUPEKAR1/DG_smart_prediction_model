---
wave: 1
depends_on: []
files_modified: [
  "backend/src/models/Result.js",
  "backend/src/models/Exam.js",
  "backend/src/controllers/resultController.js",
  "backend/src/routes/result.js"
]
autonomous: true
requirements_addressed: [T-CORE-04]
---

# Plan 09-01: Enhanced Academic Engine & Lifecycle

Implement the Draft-to-Published lifecycle and weighting features in the backend.

<objective>
To update the DB schema and models to support `status` (draft/published) and multiple grade components (internal/external) for the result engine, preventing premature visibility to parents.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/Result.js", "backend/src/models/Exam.js"] </read_first>
<action> Update `backend/src/models/Result.js` to support `status` column (default 'draft'). Modify `getAnalytics` to EXCLUDE draft results for Parent-role requests. </action>
<acceptance_criteria> Parents cannot see results until they are published. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/controllers/resultController.js"] </read_first>
<action> Create a `publishResults` method in `resultController.js` that mass-updates a given `class_id` and `exam_id` to 'published'. Hook this to `NotificationService.broadcastToTopic` to alert parents. </action>
<acceptance_criteria> Publishing changes status and triggers FCM alerts. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Enhance grading logic in `Result.js` to accept `internal_marks` and `external_marks`. Calculate total automatically based on a configured ratio (e.g. 20/80). </action>
<acceptance_criteria> The grading engine supports standard multi-component academic requirements. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/routes/result.js"] </read_first>
<action> Add the new endpoints: POST `/api/result/publish` (Teacher/Admin only) and GET `/api/result/teacher/audit` (returns all results including drafts). </action>
<acceptance_criteria> Teacher routes provide full visibility. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Verify that fetching analytics with a 'Parent' role returns empty if results are in Draft.
2. Verify that hitting the publish endpoint successfully updates rows and triggers push alerts.
3. Test that internal 15/20 and external 75/80 are summed and graded correctly (90/100 -> A+).
</verification>

<must_haves>
- Data Security: Strict enforcement of the Draft/Published flags.
- Legacy Support: Existing bulk insertion logic (from Phase 6) must default to Draft.
- Logging: Ensure notifications are fired upon publishing.
</must_haves>
