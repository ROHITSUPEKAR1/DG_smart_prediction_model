---
wave: 2
depends_on: ["03-01"]
files_modified: [
  "mobile_app/lib/features/parent/views/parent_dashboard_view.dart",
  "mobile_app/lib/features/parent/providers/parent_children_provider.dart",
  "mobile_app/lib/features/parent/widgets/child_switcher_bar.dart",
  "mobile_app/lib/features/parent/widgets/hero_attendance_card.dart",
  "mobile_app/lib/features/parent/widgets/quick_action_grid.dart"
]
autonomous: true
requirements_addressed: [P-CORE-01]
---

# Plan 03-02: Parent Dashboard UI

Implement the core multi-child dashboard for parents in Flutter.

<objective>
To create a high-fidelity, intuitive dashboard for parents that offers seamless switching between children and instant status insights.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/core/theme/app_theme.dart"] </read_first>
<action> Setup `mobile_app/lib/features/parent/providers/parent_children_provider.dart` with Riverpod `StateNotifier` that calls `GET /api/parent/children`. </action>
<acceptance_criteria> `parentChildrenProvider` handles the list of linked students and the current "selected" index. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/parent/widgets/child_switcher_bar.dart` with a horizontal avatar scroller. </action>
<acceptance_criteria> Tapping an avatar updates the global "selected child" state and triggers a UI refresh. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/parent/widgets/hero_attendance_card.dart` displaying the current status (Present/Absent/Pending). </action>
<acceptance_criteria> Hero card uses the Parent Rose-Pink #f093fb theme and displays a visual 'P' or 'A' badge. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/parent/widgets/quick_action_grid.dart` as a 2x2 scroller with navigation to specific reports. </action>
<acceptance_criteria> Action cards (Fees, HW, Results, Attendance) are clearly visible with standard icons. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/app.dart"] </read_first>
<action> Finalize `mobile_app/lib/features/parent/views/parent_dashboard_view.dart`. Combine Switcher, Hero Card, Action Grid, and Profile Header. </action>
<acceptance_criteria> Dashboard initializes on parent login and displays the correct theme for the role. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that selecting the 2nd child in the top bar updates the hero card metadata instantly.
2. verify that parent primary theme #f093fb is correctly applied across all dashboard components.
3. confirm that dash widgets correctly handle the 'Initial Loading' and 'Error' states gracefully.
</verification>

<must_haves>
- Instant Switching: Navigating between siblings MUST be liquid-smooth without full-page reloads.
- Visual peace-of-mind: The hero card should be the first thing a parent sees to confirm attendance status.
- Rose-Pink Branding: Settle into the parent identity through color and warm typography.
</must_haves>
