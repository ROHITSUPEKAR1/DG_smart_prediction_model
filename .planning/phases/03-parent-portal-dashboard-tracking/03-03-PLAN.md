---
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [
  "mobile_app/lib/features/parent/views/attendance_history_view.dart",
  "mobile_app/lib/features/parent/widgets/attendance_weekly_scroller.dart",
  "mobile_app/lib/features/parent/widgets/attendance_status_card.dart"
]
autonomous: true
requirements_addressed: [P-CORE-02]
---

# Plan 03-03: Attendance History UI

Implement the core attendance tracking interface for parents.

<objective>
To design a high-fidelity, interactive attendance tracking system that allows parents to see daily history and trends for their child.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/parent/providers/parent_children_provider.dart"] </read_first>
<action> Setup `mobile_app/lib/features/parent/widgets/attendance_weekly_scroller.dart` showing a 7-day strip layout. </action>
<acceptance_criteria> Weekly scroller displays dates and color-coded status circles (Green for P, Red for A). </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/parent/widgets/attendance_status_card.dart` for individual historical days. </action>
<acceptance_criteria> Card shows date, status label (Present/Absent/Late), and recorded time if available. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/parent/widgets/attendance_status_card.dart"] </read_first>
<action> Build `mobile_app/lib/features/parent/views/attendance_history_view.dart` as a list of recent records. </action>
<acceptance_criteria> Navigating from the action grid -> Attendance Report opens this view with the correct student ID. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement multi-status support: confirm "Late" and "Leave" icons are distinct. </action>
<acceptance_criteria> Status icons are instantly recognizable to the parent. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that selecting a day in the weekly scroller opens or scrolls to that specific entry.
2. confirm that parent-specific branding is consistent on the history page.
3. confirm that total percentage (e.g. 92% attendance) is calculated and shown.
</verification>

<must_haves>
- Progressive Insight: Summary first (Hero/Weekly scroller), followed by detail (history list).
- Clear Trends: Use colors (Green/Red/Amber) to immediately communicate student consistency.
- Responsive History: Ensure the list is performant for large semester-wide history.
</must_haves>
