---
wave: 1
depends_on: []
files_modified: [
  "backend/src/models/Parent.js",
  "backend/src/controllers/parentController.js",
  "backend/src/routes/parent.js"
]
autonomous: true
requirements_addressed: [P-CORE-01]
---

# Plan 03-01: Multi-Child Backend

Implement the database relationships and endpoints for parents to discover and manage multiple students.

<objective>
To create a secure API for parents to retrieve linked student metadata across multiple school divisions.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/BaseModel.js"] </read_first>
<action> Create `Parent.js` model. Implement `getParentChildren(parentId)` that joins `parent_student_mapping` with `students` and `classes`. </action>
<acceptance_criteria> `Parent.getParentChildren` returns all linked students for a given parent ID, including their name, roll number, and class details. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `backend/src/controllers/parentController.js`. Add `getChildren` method. </action>
<acceptance_criteria> `GET /api/parent/children` returns a JSON object containing the list of all students linked to the authenticated parent. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/models/Attendance.js"] </read_first>
<action> Add `getStudentRecentAttendance(studentId, days)` to fetch historical status for a student. </action>
<acceptance_criteria> Endpoint returns the last N records with status and date for the specific student. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/app.js"] </read_first>
<action> Mount the parent-portal routes protected by `auth` and `requireRole('parent')`. </action>
<acceptance_criteria> Parent endpoints are accessible behind the Bearer token. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Mock parent login and call `GET /api/parent/children`. Verify all siblings are returned correctly.
2. verify that retrieving student data across schools returns zero results (isolation check).
3. Test that parent cannot access data for students not linked to their ID.
</verification>

<must_haves>
- Parent-Student mapping: Ensure strict verification of the link between parent user and student entities.
- Consolidated discovery: Fetch all children metadata in a single initial request for snappy UI performance.
</must_haves>
