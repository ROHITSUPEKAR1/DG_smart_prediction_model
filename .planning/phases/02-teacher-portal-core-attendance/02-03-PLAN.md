---
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: [
  "mobile_app/lib/features/teacher/views/attendance_marking_view.dart",
  "mobile_app/lib/features/teacher/widgets/attendance_student_card.dart",
  "mobile_app/lib/features/teacher/widgets/attendance_grid_tally.dart",
  "mobile_app/lib/features/teacher/providers/attendance_marking_provider.dart"
]
autonomous: true
requirements_addressed: [T-CORE-02]
---

# Plan 02-03: Attendance Marking UI

Implement the core student marking interface for real-time attendance.

<objective>
To design a high-fidelity, interactive attendance marking system that allows teachers to record status for multiple students in seconds.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/teacher/providers/teacher_schedule_provider.dart"] </read_first>
<action> Setup `mobile_app/lib/features/teacher/providers/attendance_marking_provider.dart` with Riverpod `StateNotifier` that manages the class attendance list state (P/A/L/LV). </action>
<acceptance_criteria> `attendanceMarkingProvider` correctly toggle status between "Present" (default) and "Absent" on tap. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/widgets/attendance_student_card.dart` with a student photo (placeholder/avatar) and status badges. </action>
<acceptance_criteria> Card toggles between colors for Present (Purple/Light) and Absent (Red/Solid) instantly on tap. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/widgets/attendance_student_card.dart"] </read_first>
<action> Build `mobile_app/lib/features/teacher/widgets/attendance_grid_tally.dart` to display all students in the class. Switch between Grid and List based on class size (>20). </action>
<acceptance_criteria> Grid and List show the same students with synced markers. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Finalize `mobile_app/lib/features/teacher/views/attendance_marking_view.dart`. Combine Grid/List with a "Finalize Attendance" action button. </action>
<acceptance_criteria> Navigating from a dashboard class-item opens this view. Clicking Finalize calls `POST /api/attendance/submit`. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/views/attendance_marking_view.dart"] </read_first>
<action> Implement offline caching for marking: local persistence if submission fails. </action>
<acceptance_criteria> Submission uses a retry logic or queues locally if offline. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that selecting a student as "Absent" immediately updates the local UI and tally count.
2. verify that "Finalize Attendance" sends the correct JSON payload to the backend.
3. Test layout on small devices to ensure the grid doesn't overlap or break.
</verification>

<must_haves>
- Fast-Tally Grid: Optimized for quick one-tap marking.
- Batch Finalization: Ensure all statuses are sent in one payload.
- Real-time Total: Display 'Total Present' and 'Total Absent' counts dynamically on screen.
</must_haves>
