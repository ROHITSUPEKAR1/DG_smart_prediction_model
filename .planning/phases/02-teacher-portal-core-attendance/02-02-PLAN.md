---
wave: 2
depends_on: ["02-01"]
files_modified: [
  "mobile_app/lib/features/teacher/views/teacher_dashboard_view.dart",
  "mobile_app/lib/features/teacher/providers/teacher_schedule_provider.dart",
  "mobile_app/lib/features/teacher/widgets/timeline_schedule_card.dart",
  "mobile_app/lib/features/teacher/widgets/stats_summary_row.dart"
]
autonomous: true
requirements_addressed: [T-ACT-01]
---

# Plan 02-02: Teacher Dashboard UI

Implement the core interactive dashboard for teachers in Flutter.

<objective>
To create a high-fidelity, timeline-integrated dashboard that allows teachers to see their daily schedule and navigate quickly to current class tasks.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/core/theme/app_theme.dart"] </read_first>
<action> Setup `mobile_app/lib/features/teacher/providers/teacher_schedule_provider.dart` with Riverpod `FutureProvider` that calls `GET /api/attendance/classes`. </action>
<acceptance_criteria> `teacherScheduleProvider` returns a list of classes for the logged-in teacher. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/widgets/stats_summary_row.dart` with horizontal scroll cards (Avg Attendance, Today's Classes, Pending HW). </action>
<acceptance_criteria> Summary row is responsive and uses Teacher primary color #667eea. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/teacher/widgets/timeline_schedule_card.dart` as a vertical item in the dashboard. Highlighting the current class-duration. </action>
<acceptance_criteria> Current period tile shows a "NOW" badge with an active progress bar. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/app.dart"] </read_first>
<action> Finalize `mobile_app/lib/features/teacher/views/teacher_dashboard_view.dart`. Combine Stats Summary, Schedule Timeline, and Profile Header. </action>
<acceptance_criteria> Dashboard initializes on teacher login and displays the daily timeline correctly. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Run app and confirm teacher dashboard correctly loads current-period highlights.
2. confirm navigation from login -> dashboard correctly preserves teacher primary theme.
3. Test empty schedule state (e.g. on holidays) shows a clean fallback message.
</verification>

<must_haves>
- Interactive Timeline: Vertical flow of classes with "Now" highlighting.
- Summary Stats: Visual callouts for key daily metrics.
- Seamless Transition: Transitions from splash -> login -> dashboard should be liquid smooth.
</must_haves>
