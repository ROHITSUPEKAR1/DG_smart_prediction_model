---
wave: 1
depends_on: []
files_modified: [
  "backend/src/models/Class.js",
  "backend/src/models/Student.js",
  "backend/src/models/Attendance.js",
  "backend/src/controllers/attendanceController.js",
  "backend/src/routes/attendance.js"
]
autonomous: true
requirements_addressed: [T-CORE-01]
---

# Plan 02-01: Attendance Backend

Implement the database entities and endpoints for retrieving classes, students, and marking attendance.

<objective>
To build a scalable backend API for teachers to see their assigned class schedules and record student attendance with multi-tenant isolation.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/BaseModel.js"] </read_first>
<action> Create models `Class.js`, `Student.js`, and `Attendance.js` extending `BaseModel`. Define relationships (Class -> many Students). </action>
<acceptance_criteria> Models support scoped queries; `Attendance.js` allows bulk insertion of status updates. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `backend/src/controllers/attendanceController.js`. Add `getTeacherClasses` to list assigned schedules for the logged-in teacher. </action>
<acceptance_criteria> `GET /api/attendance/classes` returns an array of scoped classes for the authenticated teacher. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/models/Student.js"] </read_first>
<action> Add `getClassStudents(classId)` to fetch the list of students for a specific class-division. </action>
<acceptance_criteria> Endpoint returns correctly scoped student names and IDs for the given class. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/models/Attendance.js"] </read_first>
<action> Implement `submitAttendance` in the controller. Handles a batch of student statuses (P/A/L/LV) for a class-period date. </action>
<acceptance_criteria> `POST /api/attendance/submit` successfully persists attendance records in the `attendance` table with `school_id` and `teacher_id`. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/app.js"] </read_first>
<action> Mount the new attendance routes in `app.js`. </action>
<acceptance_criteria> New routes are accessible behind JWT authentication. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Mock teacher login and call `GET /api/attendance/classes` to check filtering.
2. Verify batch submission `POST /api/attendance/submit` persists multiple students correctly.
3. Confirm that querying student data across schools returns zero results (isolation check).
</verification>

<must_haves>
- Batch Submissions: Single request to mark a whole class as "Present" with selective "Absent" changes.
- Scoped Data: Only return classes and students for the current `school_id`.
- Teacher-Class mapping: Ensure only assigned teachers can mark for a specific class.
</must_haves>
