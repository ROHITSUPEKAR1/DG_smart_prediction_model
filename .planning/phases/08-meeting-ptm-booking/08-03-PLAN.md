---
wave: 3
depends_on: ["08-01", "08-02"]
files_modified: [
  "mobile_app/lib/features/teacher/views/teacher_meeting_inbox.dart",
  "mobile_app/lib/features/teacher/widgets/meeting_action_card.dart",
  "mobile_app/lib/features/teacher/providers/meeting_approval_provider.dart"
]
autonomous: true
requirements_addressed: [P-ACT-04]
---

# Plan 08-03: Teacher Meeting Management & Approvals

Implement the teacher-side management interface for accepting or rejecting meeting requests.

<objective>
To design a dedicated "Meeting Inbox" for teachers to triage incoming parent requests and manage their confirmed schedule.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/shared/providers/meeting_scheduler_provider.dart"] </read_first>
<action> Setup `mobile_app/lib/features/teacher/providers/meeting_approval_provider.dart` with a Riverpod `StateNotifier` that handles `accept` and `reject` actions. </action>
<acceptance_criteria> `meetingApprovalProvider` performs correctly formatted API calls for status updates. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/views/teacher_meeting_inbox.dart`. A vertical list of "Pending" requests and "Confirmed" upcoming meetings. </action>
<acceptance_criteria> View is linked from the Teacher Dashboard 'Meetings' action. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/teacher/widgets/meeting_action_card.dart`. A premium card showing the parent name, student name, and buttons for "APPROVE" or "REJECT (Reason)". </action>
<acceptance_criteria> Card also includes the requested time and notes. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/widgets/teacher_quick_actions.dart"] </read_first>
<action> Link the "Exams" or a new "Meetings" card to the `TeacherMeetingInbox`. </action>
<acceptance_criteria> Navigation provides a clean entry point. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that hitting 'APPROVE' correctly updates the status to 'confirmed' and re-renders the inbox.
2. verify that rejecting a meeting removes it from the 'Pending' list.
3. confirm that upcoming confirmed meetings show a 'Join (Virtual)' link if present.
</verification>

<must_haves>
- Action Feedback: Visual confirmation after approving/rejecting.
- Distinction: Clear separation between Pending and Confirmed tabs.
- Priority: Highlight meetings starting in the next 24 hours.
</must_haves>
