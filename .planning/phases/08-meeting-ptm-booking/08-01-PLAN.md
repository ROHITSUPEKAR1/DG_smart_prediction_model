---
wave: 1
depends_on: []
files_modified: [
  "backend/src/models/Meeting.js",
  "backend/src/controllers/meetingController.js",
  "backend/src/routes/meeting.js",
  "backend/src/cron/meetingReminders.js"
]
autonomous: true
requirements_addressed: [P-ACT-04]
---

# Plan 08-01: Meeting Scheduler Backend

Implement the meeting appointment lifecycle and proximity reminders.

<objective>
To create a robust scheduling backend for Parent-Teacher Meetings (PTMs) with status tracking (Pending → Approved → Completed) and 1-hour pre-meeting alerts.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/BaseModel.js"] </read_first>
<action> Create `backend/src/models/Meeting.js`. Include fields: `parent_id`, `teacher_id`, `start_time`, `status`, `notes`, and `meeting_link`. </action>
<acceptance_criteria> `Meeting` model supports role-based scoping (Parent vs Teacher). </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `backend/src/controllers/meetingController.js`. Add `requestMeeting`, `updateMeetingStatus` (Teacher-only), and `getStudentMeetings`. </action>
<acceptance_criteria> Status changes logic handles state transitions: `PENDING` -> `APPROVED`/`REJECTED`. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/services/notificationService.js"] </read_first>
<action> Implement `backend/src/cron/meetingReminders.js`. A periodic scanner that IDs all meetings starting in ~60 mins and triggers `notificationService.sendPush`. </action>
<acceptance_criteria> Automated reminders sent to both sides. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/app.js"] </read_first>
<action> Mount meeting routes and initialize the reminder cron job. </action>
<acceptance_criteria> Routes are role-locked. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that requesting a meeting as a parent creates a row in the DB with 'PENDING' status.
2. verify that a teacher's 'APPROVE' action updates the state and sends an alert.
3. verify that the cron logic correctly IDs a meeting starting in exactly 1 hour.
</verification>

<must_haves>
- Status State Machine: Prevent invalid transitions (e.g. from REJECTED back to PENDING).
- Automated FCM Hook: Reminders must be logged as notifications in the inbox.
- Scoping: Attendance to meetings is strictly filtered by student child-parent links.
</must_haves>
