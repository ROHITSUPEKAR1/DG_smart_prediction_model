---
wave: 2
depends_on: ["08-01"]
files_modified: [
  "mobile_app/lib/features/shared/views/meeting_calendar_view.dart",
  "mobile_app/lib/features/parent/widgets/meeting_request_bottom_sheet.dart",
  "mobile_app/lib/features/shared/providers/meeting_scheduler_provider.dart"
]
autonomous: true
requirements_addressed: [P-ACT-04]
---

# Plan 08-02: Multi-tenant Calendar UI (Mobile)

Implement the table calendar and booking interface for parents and teachers.

<objective>
To build a premium-styled scheduling calendar using `table_calendar` that allows parents to request meeting slots and teachers to manage their availability.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/pubspec.yaml"] </read_first>
<action> Add `table_calendar` to `pubspec.yaml`. </action>
<acceptance_criteria> Package correctly integrated and linked. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Create `mobile_app/lib/features/shared/providers/meeting_scheduler_provider.dart`. Setup Riverpod `FutureProvider` that manages the list of upcoming/pending meetings. </action>
<acceptance_criteria> `meetingSchedulerProvider` provides a `Map<DateTime, List<Meeting>>` for the calendar. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/shared/views/meeting_calendar_view.dart`. Design with signature gradients and specific day-cell styling for "Has Meeting". </action>
<acceptance_criteria> Calendar highlights days with confirmed appointments. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/parent/widgets/meeting_request_bottom_sheet.dart`. A clean modal with Time Picker and Subject/Notes fields. </action>
<acceptance_criteria> Parents can submit a request with a single tap. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/parent/widgets/quick_action_grid.dart"] </read_first>
<action> Link the "Meetings" card to the `MeetingCalendarView`. </action>
<acceptance_criteria> Dashboard navigation works correctly. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that selecting a day on the calendar updates the list of meetings shown below.
2. verify that hitting "Request" in the bottom sheet sends the correct start_time to the backend.
3. verify that confirmation color (Green/Blue) is distinct from pending color (Amber).
</verification>

<must_haves>
- Premium Styling: Usage of the DG Smart Rose-Pink and Purple gradients in calendar headers.
- Interactive Feedback: Day-tapping shows meeting details immediately.
- Time Validation: Prevent requesting meetings in the past.
</must_haves>
