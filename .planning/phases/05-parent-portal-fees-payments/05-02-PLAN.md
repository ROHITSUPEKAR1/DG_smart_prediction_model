---
wave: 2
depends_on: ["05-01"]
files_modified: [
  "mobile_app/lib/features/parent/views/fee_ledger_view.dart",
  "mobile_app/lib/features/parent/widgets/installment_status_card.dart",
  "mobile_app/lib/features/parent/providers/fee_ledger_provider.dart"
]
autonomous: true
requirements_addressed: [P-CORE-03]
---

# Plan 05-02: Fee Dashboard & Status UI

Implement the core fee management interface for parents in Flutter.

<objective>
To create a high-fidelity, interactive fee ledger UI that displays installment statuses using the Parent Rose-Pink theme with financial status indicators.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/parent/providers/parent_children_provider.dart"] </read_first>
<action> Setup `mobile_app/lib/features/parent/providers/fee_ledger_provider.dart` with a Riverpod `FutureProvider` that fetches all installments for the currently selected child. </action>
<acceptance_criteria> `feeLedgerProvider` returns a List of Fee objects with status and amount. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/parent/views/fee_ledger_view.dart` using a vertical scroll of installments. Display a "Total Balance" header at the top. </action>
<acceptance_criteria> Header shows a prominent total due in Rose-Pink. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/parent/widgets/installment_status_card.dart` displaying the installment name, due date, and amount. </action>
<acceptance_criteria> Visual coloring according to status: **Paid (Soft Green)**, **Due (Standard)**, **Overdue (Muted Red)**. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/parent/widgets/quick_action_grid.dart"] </read_first>
<action> Link the "Fees" action button on the dashboard to this new view. </action>
<acceptance_criteria> Navigation from Dashboard -> Fee Ledger is wired and passes current child context. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Run app and confirm the fee ledger loads correctly for the selected child.
2. verify that switching between siblings in the dashboard also correctly updates the fee ledger data.
3. confirm that if no balance is due, a "Clear" status appears.
</verification>

<must_haves>
- Status-Based Color Palette: Green (Paid), Red (Overdue).
- Total Balance Header: Instant financial clarity.
- Interactive Cards: Tap an installment to see payment details.
</must_haves>
