---
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: [
  "mobile_app/lib/features/parent/views/payment_mock_view.dart",
  "mobile_app/lib/features/parent/widgets/receipt_preview_card.dart",
  "mobile_app/lib/features/parent/providers/payment_execution_provider.dart"
]
autonomous: true
requirements_addressed: [P-ACT-03]
---

# Plan 05-03: Mock Payment & Receipt Loop

Implement the mock payment experience and automated receipt generation.

<objective>
To design a high-fidelity, interactive payment simulation for parents, culminating in a downloadable PDF receipt success flow.
</objective>

<tasks>
<task>
<read_first> [] </read_first>
<action> Setup `mobile_app/lib/features/parent/providers/payment_execution_provider.dart` with a Riverpod `StateNotifier` that manages the mock transaction lifecycle (Init, Process, Success, Fail). </action>
<acceptance_criteria> `paymentExecutionProvider` correctly transitions to "Processing" state for 2+ seconds to simulate network lag. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/parent/views/payment_mock_view.dart` as a bottom sheet or screen showing the total amount and a "Confirm Payment" button. </action>
<acceptance_criteria> Clicking "Confirm" triggers the mock processor. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/parent/widgets/receipt_preview_card.dart`. A clean, printable success screen with transaction ID and installment details. </action>
<acceptance_criteria> Success screen follows the Rose-Pink premium branding. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Add the "Download Receipt" action. This will show a success notification for now (simulating PDF generation). </action>
<acceptance_criteria> Clicking "Download" shows a SnackBar "Receipt saved to downloads". </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that after a mock success, the installment status in the ledger updates to 'Paid' instantly.
2. verify that clicking "Try Again" on an (artificial) fail state works correctly.
3. Test success screen with a 'Return to Dashboard' button.
</verification>

<must_haves>
- Mock Processing: Visual "Secure Payment" spinner.
- Success Feedback: Animated checkmark and printable success screen.
- Receipt Trigger: Button for PDF download simulation.
</must_haves>
