---
wave: 1
depends_on: []
files_modified: [
  "backend/src/models/Fee.js",
  "backend/src/controllers/feeController.js",
  "backend/src/routes/fee.js"
]
autonomous: true
requirements_addressed: [P-CORE-03]
---

# Plan 05-01: Fee Ledger Backend

Implement the database entities and endpoints for tracking student fee installments.

<objective>
To build a multi-tenant fee ledger that tracks installments (Q1, Lab, etc.) for each student, allowing parents to query pending dues.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/BaseModel.js"] </read_first>
<action> Create `backend/src/models/Fee.js`. Implement `getStudentFees` to return all installments with status (Paid/Pending/Overdue). </action>
<acceptance_criteria> `Fee.js` allows querying installments by `student_id` with `school_id` isolation. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `backend/src/controllers/feeController.js`. Add `getLedger` for parents to fetch fee status for a specific child. </action>
<acceptance_criteria> `GET /api/parent/student/:id/fees` returns a structured list of installments sorted by due date. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Add `recordPayment` controller to handle the "Finalize" signal from the mobile app (marking the ledger as PAID). </action>
<acceptance_criteria> `POST /api/parent/student/:id/fees/:installment_id/pay` correctly updates the DB status and records transaction metadata. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/app.js"] </read_first>
<action> Mount fee routes in `app.js` under the parent auth scope. </action>
<acceptance_criteria> New routes are role-locked to 'parent' only. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Mock parent login and call `GET /api/parent/student/:id/fees`. verify JSON list of installments.
2. confirm that installments past their due date are marked 'Overdue' in the logic.
3. Test multi-tenant isolation: Parent A cannot see Student B's fees.
</verification>

<must_haves>
- Installment Organization: Discrete line-items by name/type.
- Overdue Logic: Dynamic status tagging based on the current system date.
- Isolation: Multi-tenant scoping for all queries.
</must_haves>
