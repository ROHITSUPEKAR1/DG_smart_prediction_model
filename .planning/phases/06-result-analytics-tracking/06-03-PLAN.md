---
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: [
  "mobile_app/lib/features/parent/views/academic_analytics_view.dart",
  "mobile_app/lib/features/parent/widgets/subject_radar_chart.dart",
  "mobile_app/lib/features/parent/widgets/exam_trend_line.dart",
  "mobile_app/lib/features/parent/providers/result_analytics_provider.dart"
]
autonomous: true
requirements_addressed: [P-ACT-02]
---

# Plan 06-03: Parent Academic Analytics

Implement the radial and trend charts for academic tracking in the Parent App.

<objective>
To design an interactive academic dashboard for parents, turning raw student marks into visual infographics using `fl_chart`.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/parent/providers/parent_children_provider.dart"] </read_first>
<action> Setup `mobile_app/lib/features/parent/providers/result_analytics_provider.dart` with a Riverpod `FutureProvider` that fetches historical performance stats for a student. </action>
<acceptance_criteria> `resultAnalyticsProvider` returns detailed per-subject marks and percentages. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/parent/views/academic_analytics_view.dart` showing a vertical scroll with different chart segments (Subject Radar, Progress Trend). </action>
<acceptance_criteria> View entry is via the 'Results' dashboard grid button. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/parent/widgets/subject_radar_chart.dart` (using `fl_chart` RadarChart) showing the student's percentage across 5-6 core subjects. </action>
<acceptance_criteria> Radar correctly highlights subject strengths (e.g., 90% in Math). </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/parent/widgets/exam_trend_line.dart` (using `fl_chart` LineChart) showing the aggregate percentage growth over past exams. </action>
<acceptance_criteria> Trend line shows growth or decline across Terms. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that switching siblings correctly updates the academic charts for the selected child.
2. verify that clicking a point on the trend line reveals the specific Exam name and percentage.
3. verify that "Clear Report Card" view is also provided as a simple list below the charts.
</verification>

<must_haves>
- Interactive Charts: Tapping chart segments reveals detailed stats.
- Comparative Benchmarks: Visual markers for Class Average on the radar (optional layering).
- Trend Visibility: Directional analysis of student progress.
</must_haves>
