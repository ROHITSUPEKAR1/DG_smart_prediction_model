---
wave: 2
depends_on: ["06-01"]
files_modified: [
  "mobile_app/lib/features/teacher/views/marks_entry_view.dart",
  "mobile_app/lib/features/teacher/widgets/student_marks_row.dart",
  "mobile_app/lib/features/teacher/providers/marks_batch_provider.dart"
]
autonomous: true
requirements_addressed: [T-ACT-01]
---

# Plan 06-02: Spreadsheet Marks Entry UI

Implement the high-speed teacher grading interface in Flutter.

<objective>
To build a batch-grading interface that allows teachers to mark an entire class of 40+ students at once using inline spreadsheet-style inputs.
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/teacher/providers/teacher_schedule_provider.dart"] </read_first>
<action> Setup `mobile_app/lib/features/teacher/providers/marks_batch_provider.dart` with a Riverpod `StateNotifier` that manages the Map of student IDs to their marks. </action>
<acceptance_criteria> `marksBatchProvider` allows adding, removing, and mass-updating marks. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Build `mobile_app/lib/features/teacher/views/marks_entry_view.dart` with a vertical ListView of students and a "Subject/Max Marks" header. </action>
<acceptance_criteria> Interface shows student roll numbers, names, and an input box for marks. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `mobile_app/lib/features/teacher/widgets/student_marks_row.dart`. A thin, efficient row with the student's name and an `OutlineInputBorder` number field. </action>
<acceptance_criteria> `onChanged` in the input field updates the Riverpod state instantly. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/views/teacher_dashboard_view.dart"] </read_first>
<action> Link the "Marks" action button on the dashboard to this new view. </action>
<acceptance_criteria> Dashboard navigation correctly passes the class ID context. </acceptance_criteria>
</task>
</tasks>

<verification>
1. confirm that entering a mark for Student A updates the Riverpod state correctly.
2. verify that hitting "Submit All" triggers a single batch API call.
3. confirm that if marks exceed Max Marks (e.g. 105/100), an error state is visible.
</verification>

<must_haves>
- Inline Inputs: No modals for individual student entries.
- Batch Submission: A single button to save the entire class.
- Validation: Check raw score < Max Marks.
</must_haves>
