---
wave: 1
depends_on: []
files_modified: [
  "backend/src/models/Exam.js",
  "backend/src/models/Result.js",
  "backend/src/controllers/resultController.js",
  "backend/src/routes/result.js"
]
autonomous: true
requirements_addressed: [T-ACT-01]
---

# Plan 06-01: Exam & Result Backend

Implement the marks entry and result tracking backend with automated grading.

<objective>
To create a relational structure for Exam sessions (Unit Tests, Finals) and allow teachers to bulk-submit marks for students in their assigned sections.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/BaseModel.js"] </read_first>
<action> Create `backend/src/models/Exam.js`. Define `getExamsBySchool` to return active terms and subjects. </action>
<acceptance_criteria> `Exam` model returns hierarchical exam taxonomy (Term -> Type -> Subject). </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Create `backend/src/models/Result.js`. Implement `bulkUpsertResults` to save/update multiple student marks in one transaction. Include a `calculateGrade` utility inside the class. </action>
<acceptance_criteria> `calculateGrade` correctly maps percentage to (A+, A, B, C...) and marks are saved with `school_id` isolation. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Implement `backend/src/controllers/resultController.js`. Add `postMarks` (Teacher) and `getAnalytics` (Parent). </action>
<acceptance_criteria> `getAnalytics` calculates **Class Average** and **Highest Mark** for each subject on-the-fly or returns pre-calculated stats. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/app.js"] </read_first>
<action> Mount result routes in `app.js` under both teacher and parent role scopes as needed. </action>
<acceptance_criteria> Routes are role-locked based on target functionality (Entry vs. Viewing). </acceptance_criteria>
</task>
</tasks>

<verification>
1. Mock teacher login and call `POST /api/marks/bulk`. confirm multiple students' marks are updated at once.
2. verify that 95% is auto-assigned "A+" in the DB record.
3. verify that parents can fetch their child's results and see the benchmark (average/highest) data for context.
</verification>

<must_haves>
- Bulk Upsert: Use standard DB transactions for marking integrity.
- Automated Grading: Letter grade calculation at the model level.
- Smart Aggregation: Benchmarking logic for class-wide context.
</must_haves>
