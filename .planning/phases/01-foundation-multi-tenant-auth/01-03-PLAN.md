---
wave: 1
depends_on: []
files_modified: [
  "mobile_app/pubspec.yaml",
  "mobile_app/lib/main.dart",
  "mobile_app/lib/app.dart",
  "mobile_app/lib/core/theme/app_theme.dart",
  "mobile_app/lib/core/routes/app_routes.dart",
  "mobile_app/lib/features/auth/providers/auth_provider.dart",
  "mobile_app/lib/features/auth/views/login_view.dart",
  "mobile_app/lib/features/auth/views/splash_view.dart"
]
autonomous: true
requirements_addressed: [AUTH-01, AUTH-02]
---

# Plan 01-03: Mobile Foundation

Setup the Flutter application, design system, and initial role-based login screens.

<objective>
To establish a premium, state-of-the-art Flutter app base that supports multiple user personas (Teacher/Parent) with an interactive, responsive login experience.
</objective>

<tasks>
<task>
<read_first> [] </read_first>
<action> Initialize a new Flutter project in `mobile_app/` and add dependencies: `flutter_riverpod`, `dio`, `flutter_secure_storage`, `shared_preferences`, `google_fonts`, `fluro` (or `go_router`), `flutter_svg`, `shimmer`, `freezed` (optional), `json_serializable`. </action>
<acceptance_criteria> `mobile_app/pubspec.yaml` exists with all listed dependencies. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/pubspec.yaml"] </read_first>
<action> Implement `mobile_app/lib/core/theme/app_theme.dart` with custom color schemes for Teacher (Purple #667eea) and Parent (Rose-Pink #f093fb). Use "Outfit" or "Inter" from Google Fonts. </action>
<acceptance_criteria> Theme data provides light/dark mode and role-specific primary colors. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/core/theme/app_theme.dart"] </read_first>
<action> Setup basic routing in `mobile_app/lib/core/routes/app_routes.dart` and the base `App` widget in `app.dart` with Riverpod `ProviderScope`. </action>
<acceptance_criteria> App initializes correctly with Splash view as the entry point. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/app.dart"] </read_first>
<action> Build `mobile_app/lib/features/auth/views/login_view.dart` with a Segmented Controller (Teacher | Parent) and a responsive form (Mobile #, OTP, Password). </action>
<acceptance_criteria> Login UI theme switches instantly (colors and icons) when a role tab is selected. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/auth/views/login_view.dart"] </read_first>
<action> Create `mobile_app/lib/features/auth/providers/auth_provider.dart` using `AsyncNotifier` to handle the multi-step login flow (Request OTP -> Verify -> Store JWT). </action>
<acceptance_criteria> Auth provider can handle "Idle", "Loading", "OTPSent", "Authenticated", and "Error" states. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Run `flutter test` (or basic UI test) to verify routing and theme switching.
2. Verify that `flutter_secure_storage` is invoked after a mock successful login.
3. Check UI responsiveness and contrast accessibility for both roles.
</verification>

<must_haves>
- Premium aesthetics: High-quality typography, smooth transitions, and distinct role brand colors.
- State-of-the-art Riverpod logic: Clean separation of UI and business logic.
- Mobile first: Touch-friendly targets and responsive layout for different screen sizes.
</must_haves>
