---
wave: 1
depends_on: []
files_modified: [
  "backend/package.json",
  "backend/src/app.js",
  "backend/src/config/db.js",
  "backend/src/middleware/schoolScope.js",
  "backend/src/middleware/auth.js",
  "backend/src/models/BaseModel.js",
  "backend/.env.example"
]
autonomous: true
requirements_addressed: [AUTH-03, AUTH-04]
---

# Plan 01-01: Backend Foundation

Setup the multi-tenant Node.js + Express backend with Knex.js query builder and scoping middleware.

<objective>
To establish a secure, multi-tenant Express API that automatically isolates all database queries by `school_id` and provides a base for future endpoint implementation.
</objective>

<tasks>
<task>
<read_first> [] </read_first>
<action> Initialize `backend/` directory with Node.js, Express, Knex, MySQL2, and supporting libraries (dotenv, cors, jsonwebtoken, bcryptjs, helmet). </action>
<acceptance_criteria> `backend/package.json` exists with dependencies; `backend/.env.example` contains `DB_HOST`, `DB_USER`, `DB_PASS`, `DB_NAME`, `JWT_SECRET`. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Create `backend/src/config/db.js` using Knex.js with connection pooling and the `mysql2` client. Add a `withSchoolScope` helper extension to the Knex instance. </action>
<acceptance_criteria> `db.js` provides a configured Knex instance; contains `withSchoolScope` logic that adds `where('school_id', schoolId)` to queries. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/config/db.js"] </read_first>
<action> Implement `backend/src/middleware/schoolScope.js` that extracts `school_id` from the JWT or header and attaches it to `req.school_id`. </action>
<acceptance_criteria> Middleware correctly sets `req.school_id` from `X-School-ID` header or JWT payload. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/middleware/schoolScope.js"] </read_first>
<action> Setup `backend/src/app.js` with Express, Helmet for security, CORS, JSON parsing, and the global school scoping middleware. </action>
<acceptance_criteria> `backend/src/app.js` runs a health check endpoint `/api/health` returning `{ status: "ok", mode: "multi-tenant" }`. </acceptance_criteria>
</task>

<task>
<read_first> ["backend/src/config/db.js", "backend/src/middleware/schoolScope.js"] </read_first>
<action> Create `backend/src/models/BaseModel.js` that uses the `withSchoolScope` Knex instance by default for its static methods (all(), find(), where()). </action>
<acceptance_criteria> `BaseModel.js` allows derived models to perform scoped queries automatically. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Run `npm test` (or basic health script) to verify Express server starts.
2. Verify `req.school_id` is populated in a sample route using an interceptor test.
3. Confirm Knex query log includes `WHERE school_id = ?` when using the `BaseModel`.
</verification>

<must_haves>
- Multi-tenancy enforced: All queries (except global) must include `school_id`.
- Secure defaults: Helmet and CORS configured correctly for mobile clients.
- Clean Scaffolding: Folder structure based on separation of concerns (config, middleware, models, routes).
</must_haves>
