---
wave: 2
depends_on: ["10-01"]
files_modified: [
  "backend/src/routes/predictive.js",
  "backend/src/controllers/predictiveController.js",
  "backend/src/app.js",
  "mobile_app/lib/features/teacher/views/teacher_risk_hub.dart",
  "mobile_app/lib/features/teacher/widgets/risk_insight_card.dart",
  "mobile_app/lib/features/teacher/widgets/teacher_quick_actions.dart"
]
autonomous: true
requirements_addressed: [T-CORE-04]
---

# Plan 10-02: Predictive AI Insights / Teacher Triage

Surface the generated risks visually to Teachers inside the DG Smart App.

<objective>
To build endpoints exposing the `RiskAssessment` table and display a dedicated "Insights" dashboard where Teachers can triage failing vs absentee students immediately.
</objective>

<tasks>
<task>
<read_first> ["backend/src/models/RiskAssessment.js"] </read_first>
<action> Build `backend/src/controllers/predictiveController.js` and `backend/src/routes/predictive.js`. Exposed endpoint `GET /api/predictive/risks/class/:classId` delivering risk flags. </action>
<acceptance_criteria> Only flagged students are returned to minimize payload footprint. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Mount `/api/predictive` securely in `backend/src/app.js` using `teacherOnly`. </action>
<acceptance_criteria> Route is successfully mounted. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/pubspec.yaml"] </read_first>
<action> Develop `mobile_app/lib/features/teacher/views/teacher_risk_hub.dart`. Provide a filtered list (Tabs: Attendance, Academic, Fee) showing student risks. </action>
<acceptance_criteria> Filtering by Risk Type is seamless using standard Provider integration. </acceptance_criteria>
</task>

<task>
<read_first> [] </read_first>
<action> Create `mobile_app/lib/features/teacher/widgets/risk_insight_card.dart`. A warning-colored card indicating the student's name, severity rating (Low/High), the AI reason ("Dropped 3 consecutive days"), and quick actions -> `Schedule PTM`. </action>
<acceptance_criteria> Card UI utilizes Orange/Red palettes for clarity. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/teacher/widgets/teacher_quick_actions.dart"] </read_first>
<action> Add the `Insights` access button directly onto the Teacher Dashboard replacing or supplementing the generic "Resources" action. </action>
<acceptance_criteria> Dashboard navigation includes instant access to the AI triage center. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Set up a Mock response in Provider indicating an 'ACADEMIC' risk and verify the hub displays it under the corresponding tab.
2. Confirm the Quick Action routes correctly to `TeacherRiskHub`.
</verification>

<must_haves>
- Direct Escalation: Include the ability to jump to the PTM scheduler directly from the risk card to address the issue.
</must_haves>
