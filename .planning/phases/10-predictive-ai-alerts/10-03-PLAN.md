---
wave: 3
depends_on: ["10-01", "10-02"]
files_modified: [
  "mobile_app/lib/features/parent/widgets/emergency_alert_banner.dart",
  "mobile_app/lib/features/parent/providers/predictive_alert_provider.dart",
  "mobile_app/lib/features/parent/views/parent_dashboard_view.dart"
]
autonomous: true
requirements_addressed: [P-CORE-04]
---

# Plan 10-03: Active Escalation Interface (Parents)

Seamlessly alert parents to flagged risks dynamically modifying existing dashboard components.

<objective>
To update the `EmergencyAlertBanner` behavior replacing school-wide circulars with personalized, severe AI Risk warnings (e.g. "Attention: Student absent for 3 days").
</objective>

<tasks>
<task>
<read_first> ["mobile_app/lib/features/parent/widgets/emergency_alert_banner.dart"] </read_first>
<action> Refactor `EmergencyAlertBanner` to accept a parameterized `AlertType` spanning `SCHOOL_CIRCULAR`, `CRITICAL_RISK`, and `SOFT_WARNING`. Map `CRITICAL_RISK` to Red/Orange styling. </action>
<acceptance_criteria> Visual component supports context-adaptive warnings without breaking previous circular logic. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/shared/providers/notifications_provider.dart"] </read_first>
<action> Implement `mobile_app/lib/features/parent/providers/predictive_alert_provider.dart`. Fetch unacknowledged personal risks from the backend if present. </action>
<acceptance_criteria> Dedicated state management isolated for predictive risks preventing them getting lost in generic notification tables. </acceptance_criteria>
</task>

<task>
<read_first> ["mobile_app/lib/features/parent/views/parent_dashboard_view.dart"] </read_first>
<action> Update `ParentDashboardView` to inject the Highest Severity Risk into the dashboard header utilizing the refactored Banner. Fallback to generic school circulars if none exist. </action>
<acceptance_criteria> Risks override standard circulars dynamically due to strict prioritization. </acceptance_criteria>
</task>
</tasks>

<verification>
1. Mock a severe Academic Risk on the provider.
2. Ensure the parent dashboard displays a red "Attention: Academic Performance Alert" banner overriding any general school greetings.
3. Validate "Acknowledged" action drops the banner and resets to generic state.
</verification>

<must_haves>
- Prioritization Hierarchy: `Risk Banner` > `School Circular` > `Standard Greeting`.
- Panic Mitigation: Provide a direct "Contact Teacher" / "Schedule PTM" CTA button inside the warning banner to handle parent response effectively.
</must_haves>
